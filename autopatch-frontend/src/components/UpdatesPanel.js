import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const UpdatesPanel = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Updates Panel</Typography>
        <Typography>Update information will appear here.</Typography>
      </CardContent>
    </Card>
  );
};

export default UpdatesPanel;